"""E3 — ablating the deduction operator at inference (Sudoku-Extreme).

Entirely eval-only. Consumes E1 checkpoints from their fixed volume paths and
writes eval artifacts under /checkpoints/followups/e3/. See README.md.
"""
